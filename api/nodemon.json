{
  "signal": "SIGTERM",
  "env": { "NODE_ENV": "dev" },
  "ext": "ts",
  "watch": ["./**/*.ts", "!**/*.test.ts"],
  "exec": "node --no-warnings --experimental-specifier-resolution=node --loader ts-node/esm",
  "args": ["src/app.ts"]
}
